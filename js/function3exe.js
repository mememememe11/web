// function3exe.js
// 1. max(10,7) => "2개의 숫자중에서 큰값은 10 입니다"

function max(num1, num2) {
    let sum = num1 > num2 ? num1 : num2;
    return = sum;
    }

// 2. sumUpTo(5) => "1부터 5까지 합은 15입니다.

function sumUpTo {
    let sum = 0;

    for (let i = 1; i <= ; i++) {
        sum += i;
    }

    return `1부터 ${n}까지 합은 ${sum}입니다
}






// 3. diff( 17,34 ) => 두수의 차는 17 입니다




    // let sum = 0;
    // for (let i =1; i<=5; i++){
    //     sum += sum;
    // } console.log(sum);



    // for (let i = 1; i<=20; i++){
    //    if(i % 2 == 0){  //조건 : 짝수일 때만 합
    //     sum = sum + i; //누적
    //    }         
    // }console.log('sum의 값은 ' + sum );